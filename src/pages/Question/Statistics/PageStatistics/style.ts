import styled from 'styled-components';

export const PageStatisticsWrapper = styled.div``;

export const ImageLink = styled.a`
  display: block;
  margin-bottom: 4px;
  color: #1890ff;

  &:hover {
    color: #40a9ff;
  }
`;
